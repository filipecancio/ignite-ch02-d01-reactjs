name: Realizando testes automáticos
'on':
  push:
    branches:
      - dev
jobs:
  test_application:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install && npm run test
